/**
 * Temporary notice while project repositories move from GitHub to GitLab.
 *
 * To remove later:
 * 1. Set `REPO_TRANSFER_NOTICE_ENABLED` to `false`, or delete this file plus
 *    `RepoTransferNotice.astro`.
 * 2. Remove `dev.repo_transfer_notice` from `src/locales/en/translation.json`
 *    and `src/locales/it/translation.json`.
 * 3. Remove notice imports/usages in `DevelopmentTab.astro`, `ProjectGallery.tsx`,
 *    and `ProjectUI.tsx`.
 */
export const REPO_TRANSFER_NOTICE_ENABLED = true;
